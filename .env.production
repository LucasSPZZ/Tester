# Este arquivo é usado automaticamente pelo Vite durante o build de produção (npm run build)
# As variáveis aqui definidas sobrescrevem as do .env padrão em builds de produção

# URL do backend em produção
# Substitua pela URL real do seu backend deployado
VITE_BACKEND_URL=https://78f55280a27e.ngrok-free.app
